# ── 手勢 → 動作 對應表 ──────────────────────────────────────────
# HaGRID 類別名稱對應到我們定義的動作
GESTURE_ACTION_MAP = {
    "one":        "MOUSE",   # 食指朝上 → 移動滑鼠游標
    "two_up":     "SWIPE",   # 食指＋中指合併朝上 → 上下左右滑動
    "ok":         "CLICK",   # OK 手勢 → 左鍵點擊
    "stop":       "HOME",    # 張開手掌 → 顯示桌面 (Win+D)
    "no_gesture": "NONE",
}

# ── 路徑 ─────────────────────────────────────────────────────────
MODEL_PATH = "model/gesture_mlp.keras"
LABEL_PATH = "model/labels.pkl"

# ── 推論門檻 ──────────────────────────────────────────────────────
CONFIDENCE_THRESHOLD = 0.85   # 低於此信心值視為 no_gesture
COOLDOWN_SEC         = 1.0    # 觸發一次動作後的冷卻秒數

# ── 上下滾動（虛擬卡點滾輪）────────────────────────────────────
NOTCH_SIZE   = 0.025  # 每個卡點的手指位移距離（正規化 0~1，調小=更靈敏）
NOTCH_SCROLL = 8      # 每個卡點觸發的滾動格數（調大=每卡點滾更多）

# ── 左右換頁（離散觸發）─────────────────────────────────────────
SWIPE_FRAMES    = 15    # 累積幾幀後判斷左右方向
SWIPE_THRESHOLD = 0.10  # 左右位移門檻（0~1）
COOLDOWN_LR_SEC = 0.8   # 左右換頁冷卻秒數

# ── 滑鼠移動參數 ──────────────────────────────────────────────────
MOUSE_SMOOTH    = 0.4   # EMA 平滑係數（越小越平滑但反應慢，0.2~0.6）
MOUSE_MARGIN    = 0.15  # 畫面邊緣忽略區（避免手靠邊時滑鼠衝到角落）
